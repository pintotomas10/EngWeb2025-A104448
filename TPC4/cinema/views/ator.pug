extends layout

block content
  .w3-card-4
    header.w3-container.w3-light-blue
      h1= "Filmes com " + ator
    .w3-container
      br
      a.w3-btn.w3-blue.w3-mb-2.w3-right(href='/filmes') Voltar
      br
      table.w3-table-all
        tr
          th Título
        each f in filmes || []
          tr
            td: a(href=`/filmes/${encodeURIComponent(f.id)}`) #{f.id}

    footer.w3-container.w3-light-blue
      adress Generated by EW2025 class