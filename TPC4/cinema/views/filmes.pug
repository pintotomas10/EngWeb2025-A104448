extends layout

block content
  .w3-card-4
    header.w3-container.w3-light-blue
      h1= tit
    .w3-container
      br
      a.w3-btn.w3-blue.w3-mb-2.w3-right(href='/') Voltar
      br
      table.w3-table-all 
        tr 
          th Ano 
          th Titulo 
          th Elenco 
          th Géneros
        each f in lfilmes  || []
          tr 
            td= f.year
            td: a(href=`/filmes/${encodeURIComponent(f.title)}`) #{f.title}
            td
              ul.w3-ul 
                each ator in f.cast 
                  li: a(href=`/atores/${encodeURIComponent(ator)}`) #{ator}
            td 
              ul.w3-ul 
                each g in f.genres 
                  li=g 
    footer.w3-container.w3-light-blue
      adress Generated by EW2025 class